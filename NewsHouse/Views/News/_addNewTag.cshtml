@model NewsHouse.Models.Tags

<div class="mb-2">
    <div class="col-12">
        @Html.DropDownListFor(x => x.TagsId, ViewData["Tags"] as SelectList, "Select tags", htmlAttributes: new { @class = "form-select mb-1" })
    </div>
    <div class="col-2">
        <a href="#" id="btnDeleteTag" class="btn btn-sm btn-danger">Remove<i class="fa fa-trash"></i></a>
    </div>
</div>

